#pragma once
#include <iostream>
#include <random>
#include <ctime>

class Security {
public:

static std::string GenerateStrongPassowrd();
static bool IsValidAge(int age);
//bool IsStrongPassword(std::string password);
};





